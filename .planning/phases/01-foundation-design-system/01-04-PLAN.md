---
phase: 01-foundation-design-system
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase1.spec.ts
autonomous: true
gap_closure: true
requirements:
  - NAV-04
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "npx playwright test --grep @smoke exits 0 with all tests passing"
    - "NAV-04 test correctly verifies desktop nav is hidden at 375px viewport"
  artifacts:
    - path: "tests/phase1.spec.ts"
      provides: "Smoke test suite with correct NAV-04 assertion"
      contains: ".desktop-nav"
      assertion_type: "not.toBeVisible() (CSS-driven visibility check, not class attribute check)"
  key_links:
    - from: "tests/phase1.spec.ts"
      to: "src/components/Nav.astro"
      via: "locator('.desktop-nav') matching actual CSS class in Nav.astro"
      pattern: "desktop-nav"
---

<objective>
Fix the one blocker gap from Phase 1 verification: the NAV-04 smoke test references `.nav-links` (a class that does not exist in Nav.astro) instead of `.desktop-nav`. This causes 3 test failures (one per viewport project) and prevents the smoke suite from exiting 0.

Purpose: Unblock phase gate — `npx playwright test --grep @smoke` must exit 0 before Phase 2 begins.
Output: Updated `tests/phase1.spec.ts` with a corrected NAV-04 assertion that matches the actual DOM.
</objective>

<execution_context>
@/Users/mylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mylinh/projects/resume-website/.planning/PROJECT.md
@/Users/mylinh/projects/resume-website/.planning/ROADMAP.md
@/Users/mylinh/projects/resume-website/.planning/STATE.md

<interfaces>
<!-- Relevant class in Nav.astro — executor needs this to write the correct assertion -->

From src/components/Nav.astro (desktop nav element, line 10):
```html
<!-- Desktop nav links — hidden at mobile via CSS, no Tailwind utility classes on this element -->
<nav class="desktop-nav" aria-label="Main navigation">
  <a href="#about" class="nav-link">About</a>
  ...
</nav>
```

From src/components/Nav.astro (CSS, lines 79-93):
```css
.desktop-nav {
  display: none;        /* hidden by default (mobile-first) */
  align-items: center;
  gap: 2rem;
}

@media (min-width: 768px) {
  .desktop-nav {
    display: flex;      /* visible at desktop */
  }
}
```

The desktop nav element has ONLY the class `desktop-nav` — there is no `hidden` class or any Tailwind utility on this element. Hiding at mobile is done entirely by CSS (`display: none` as the default rule, overridden to `display: flex` at `min-width: 768px`).

This means the correct assertion at 375px is:
- `page.locator('.desktop-nav')` (not `.nav-links` — that class does not exist)
- Check: `not.toBeVisible()` — Playwright's visibility check reads computed CSS, which correctly sees `display: none` at 375px

Do NOT use `toHaveClass(/hidden/)` — there is no `hidden` class on this element.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix NAV-04 smoke test class name mismatch</name>
  <files>tests/phase1.spec.ts</files>
  <action>
    In `tests/phase1.spec.ts` at line 124, replace:

    ```typescript
    await expect(page.locator(".nav-links")).toHaveClass(/hidden/);
    ```

    With:

    ```typescript
    await expect(page.locator(".desktop-nav")).not.toBeVisible();
    ```

    Rationale: Nav.astro uses class `.desktop-nav`, not `.nav-links`. The element has no `hidden` class — it is hidden at mobile widths via CSS (`display: none` by default, overridden to `display: flex` at `min-width: 768px`). Playwright's `not.toBeVisible()` reads computed CSS and correctly reflects this CSS-driven visibility.

    No other changes to the test file are needed. The rest of the NAV-04 test (hamburger visibility, mobile menu open/close) correctly references `#hamburger` and `#mobile-menu` which exist in Nav.astro.
  </action>
  <verify>
    <automated>cd /Users/mylinh/projects/resume-website && npx playwright test --grep @smoke 2>&1 | tail -20</automated>
  </verify>
  <done>npx playwright test --grep @smoke exits 0 with all 10 tests passing across all 3 viewport projects (30 total test runs)</done>
</task>

</tasks>

<verification>
After the fix, run the full smoke suite:

```bash
cd /Users/mylinh/projects/resume-website && npx playwright test --grep @smoke
```

Expected: All tests pass, exit code 0, no test failures.

Also confirm the line change is correct:
```bash
grep -n "desktop-nav\|nav-links" /Users/mylinh/projects/resume-website/tests/phase1.spec.ts
```

Expected: `.nav-links` does not appear; `.desktop-nav` appears at the corrected line with `not.toBeVisible()`.
</verification>

<success_criteria>
- `npx playwright test --grep @smoke` exits 0
- Line 124 of tests/phase1.spec.ts uses `.desktop-nav` with `not.toBeVisible()`, not `.nav-links` with `toHaveClass(/hidden/)`
- No other test assertions were modified
- Phase 1 smoke gate is unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-04-SUMMARY.md`
</output>
