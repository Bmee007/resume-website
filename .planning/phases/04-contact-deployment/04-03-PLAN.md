---
phase: 04-contact-deployment
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - src/components/ContactSection.astro
autonomous: false
requirements:
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "Visitor who submits the contact form receives a success confirmation ('Message sent. I'll be in touch soon.')"
    - "Borina receives the submitted message via email (Formspree delivers it)"
    - "The form cannot be re-submitted after a successful send"
  artifacts:
    - path: "src/components/ContactSection.astro"
      provides: "Real Formspree form ID inserted (not placeholder)"
      contains: "formspree.io/f/"
  key_links:
    - from: "ContactSection.astro data-formspree-id"
      to: "https://formspree.io/f/{real-form-id}"
      via: "fetch POST in <script> block"
      pattern: "formspree.io"
---

<objective>
Activate the Formspree form with a real account and verified form ID, then perform visual end-to-end verification of the contact form and full page layout.

Purpose: The form is built but delivers email only once a real Formspree form ID replaces the placeholder. This plan gates on the human account setup, then verifies the complete Phase 4 deliverable visually.
Output: A live, email-delivering contact form on the deployed Vercel URL.
</objective>

<execution_context>
@/Users/mylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-contact-deployment/04-CONTEXT.md
@.planning/phases/04-contact-deployment/04-02-SUMMARY.md

@src/components/ContactSection.astro
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create Formspree account and activate form</name>
  <files>src/components/ContactSection.astro</files>
  <action>
    Replace the placeholder Formspree ID ("YOUR_FORM_ID") with the real form ID from the Formspree dashboard. Two occurrences must be updated:
    1. The FORMSPREE_ID constant in the script block
    2. The data-formspree-id attribute on the form element
  </action>
  <what-to-do>
    The contact form is built and all smoke tests pass, but the Formspree form ID is a placeholder ("YOUR_FORM_ID"). To make the form actually deliver email, complete these steps:

    1. Go to https://formspree.io and create a free account (or log in if you already have one)
    2. Create a new form — name it "Borina Keo Resume Contact"
    3. Check your email inbox for the Formspree verification email and click the confirmation link
    4. Copy the form ID from the Formspree dashboard (it looks like "xpwzabcd" — 8 characters after the slash in the endpoint URL)
    5. Open src/components/ContactSection.astro and replace "YOUR_FORM_ID" in two places:

    Find in the script block:
    ```
    const FORMSPREE_ID = "YOUR_FORM_ID";
    ```
    Replace with:
    ```
    const FORMSPREE_ID = "xpwzabcd";   ← your actual ID
    ```

    Also find on the form element:
    ```
    data-formspree-id="YOUR_FORM_ID"
    ```
    Replace with:
    ```
    data-formspree-id="xpwzabcd"   ← same actual ID
    ```

    Formspree free tier: 50 submissions/month — adequate for a portfolio contact form.
  </what-to-do>
  <verify>
    <automated>grep -n "YOUR_FORM_ID" /Users/mylinh/projects/resume-website/src/components/ContactSection.astro || echo "Placeholder replaced"</automated>
  </verify>
  <done>The string "YOUR_FORM_ID" no longer appears in ContactSection.astro. A real 8-character Formspree form ID is in both the FORMSPREE_ID constant and the data-formspree-id attribute.</done>
  <resume-signal>Type "done" when you've replaced the placeholder ID in ContactSection.astro</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — contact section and live form submission</name>
  <files>src/components/ContactSection.astro</files>
  <action>
    Human verification of visual appearance, error states, success state (live email delivery), social links, and mobile layout. No code changes expected unless issues are found.
  </action>
  <what-built>
    ContactSection.astro: liquid-glass card with 3-field form, Formspree AJAX submission, success/error states, LinkedIn + GitHub social links below the card, minimal footer.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open http://localhost:4321 in your browser.

    **1. Section appearance:**
    - Scroll to the bottom — confirm "Get In Touch" heading and "Open to consulting engagements and full-time opportunities" tagline appear
    - Confirm the form is inside a liquid-glass card matching the style of Skills and Projects cards
    - Confirm LinkedIn and GitHub icon links are visible below the card

    **2. Error state (client-side validation):**
    - Click "Send Message" without filling in any fields
    - Confirm red borders appear on all three fields
    - Confirm error text appears below each invalid field (e.g. "Name is required")
    - Confirm the form is still visible (no premature success state)

    **3. Success state (requires real Formspree ID from Task 1):**
    - Fill in a real name, email, and message
    - Click "Send Message"
    - Confirm the form fades out and is replaced by: "Message sent. I'll be in touch soon."
    - Confirm no submit button is visible after success (prevents re-submit)
    - Check Borina's email inbox — the submitted message should arrive from Formspree

    **4. Social links:**
    - Click the LinkedIn icon — confirm it opens linkedin.com/in/borinakeo in a new tab
    - Click the GitHub icon — confirm it opens github.com/Bmee007 in a new tab

    **5. Mobile check (375px):**
    - Resize browser to 375px width or use DevTools mobile mode
    - Confirm the form remains readable and the card doesn't overflow horizontally
  </how-to-verify>
  <verify>
    <automated>cd /Users/mylinh/projects/resume-website && npx playwright test --project=desktop 2>&1 | tail -10</automated>
  </verify>
  <done>
    All 6 phase4 smoke tests pass. Full Playwright suite is GREEN. Visual inspection confirmed: liquid-glass card renders correctly, error states show red borders and error text, success state shows correct copy, social links open correct profiles, mobile layout is not broken.
  </done>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
After human approval:

Run full Playwright suite: `npx playwright test`

All tests across all phases must pass GREEN.
</verification>

<success_criteria>
- Formspree placeholder ID replaced with real form ID from Borina's account
- Visual inspection confirms: liquid-glass card, correct copy, error states, success state
- Live form submission delivers email to Borina's inbox
- Full Playwright test suite is GREEN (no regressions)
- Phase 4 complete — ready for Phase 5 (SEO, Polish & Launch)
</success_criteria>

<output>
After completion, create `.planning/phases/04-contact-deployment/04-03-SUMMARY.md`
</output>
