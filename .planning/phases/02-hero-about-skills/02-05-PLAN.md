---
phase: 02-hero-about-skills
plan: 05
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - src/pages/index.astro
autonomous: false
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04
  - SKILL-05

must_haves:
  truths:
    - "Visiting the site shows hero, about, and skills sections all rendering with real content"
    - "No duplicate section IDs in the rendered HTML (no double #about or #skills)"
    - "All 12 Phase 2 Playwright tests pass green"
    - "Phase 1 tests still pass (nav scroll, active state, hamburger)"
    - "No horizontal overflow at 375px viewport after wiring"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Updated page importing AboutSection and SkillsSection, stubs removed"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/AboutSection.astro"
      via: "import AboutSection; <AboutSection />"
      pattern: "import AboutSection"
    - from: "src/pages/index.astro"
      to: "src/components/SkillsSection.astro"
      via: "import SkillsSection; <SkillsSection />"
      pattern: "import SkillsSection"
---

<objective>
Wire AboutSection and SkillsSection into index.astro, replacing the Phase 2 placeholder stubs. Run the full Phase 2 test suite and fix any remaining failures. Human checkpoint for visual verification.

Purpose: Makes all Phase 2 content visible on the live site. The final task of the phase confirms everything integrates correctly end-to-end.
Output: Updated index.astro + green Phase 2 test suite + visual sign-off from user.
</objective>

<execution_context>
@/Users/mylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-hero-about-skills/02-CONTEXT.md
@.planning/phases/02-hero-about-skills/02-02-SUMMARY.md
@.planning/phases/02-hero-about-skills/02-03-SUMMARY.md
@.planning/phases/02-hero-about-skills/02-04-SUMMARY.md
@src/pages/index.astro
</context>

<interfaces>
<!-- Current index.astro structure -->
```astro
---
import BaseLayout from "../layouts/BaseLayout.astro";
import HeroSection from "../components/HeroSection.astro";
---
<BaseLayout title="Borina Keo — ERP & WMS AI Integration Leader">
  <HeroSection />
  <section id="about" class="min-h-screen flex items-center justify-center">
    <p ...>About — Phase 2</p>
  </section>
  <section id="skills" class="min-h-screen flex items-center justify-center">
    <p ...>Skills — Phase 2</p>
  </section>
  <section id="experience" ...>...</section>
  <section id="projects" ...>...</section>
  <section id="contact" ...>...</section>
</BaseLayout>
```

<!-- Target index.astro structure -->
```astro
---
import BaseLayout from "../layouts/BaseLayout.astro";
import HeroSection from "../components/HeroSection.astro";
import AboutSection from "../components/AboutSection.astro";
import SkillsSection from "../components/SkillsSection.astro";
---
<BaseLayout title="Borina Keo — ERP & WMS AI Integration Leader">
  <HeroSection />
  <AboutSection />
  <SkillsSection />
  <section id="experience" ...>...</section>  <!-- Phase 3 stub — unchanged -->
  <section id="projects" ...>...</section>    <!-- Phase 3 stub — unchanged -->
  <section id="contact" ...>...</section>     <!-- Phase 4 stub — unchanged -->
</BaseLayout>
```

CRITICAL: Remove the entire `<section id="about">` and `<section id="skills">` stub blocks.
Do NOT wrap AboutSection or SkillsSection in additional section tags.
AboutSection.astro owns its own `<section id="about">`.
SkillsSection.astro owns its own `<section id="skills">`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update index.astro — replace stubs with AboutSection and SkillsSection</name>
  <files>src/pages/index.astro</files>
  <action>
Edit src/pages/index.astro:

1. Add two import statements in the frontmatter (---) block:
   ```
   import AboutSection from "../components/AboutSection.astro";
   import SkillsSection from "../components/SkillsSection.astro";
   ```

2. Replace the entire `<section id="about" ...>` stub (the element with child text "About — Phase 2") with simply:
   ```
   <AboutSection />
   ```

3. Replace the entire `<section id="skills" ...>` stub (the element with child text "Skills — Phase 2") with simply:
   ```
   <SkillsSection />
   ```

4. Leave `<section id="experience">`, `<section id="projects">`, and `<section id="contact">` stubs completely unchanged — they are Phase 3 and Phase 4 work.

The result is a clean index.astro with 5 content blocks: HeroSection, AboutSection, SkillsSection, and 3 phase stubs.

After editing, run `npx astro build 2>&1 | grep -i "duplicate\|error" | head -10` to confirm no duplicate ID warnings.
  </action>
  <verify>
    <automated>cd /Users/mylinh/projects/resume-website && npx playwright test tests/phase2.spec.ts --project=desktop 2>&1 | tail -20</automated>
  </verify>
  <done>
index.astro imports AboutSection and SkillsSection. Stub sections are removed. All 12 Phase 2 tests pass (HERO-01 through SKILL-05). No duplicate section IDs. Phase 1 tests still pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — hero, about, and skills sections</name>
  <files>none</files>
  <action>
Human verification of Phase 2 visual output. Claude has completed all automated implementation. This task pauses for the user to open the browser and confirm the sections render correctly.

Dev server is running at http://localhost:4321 (run `npm run dev` if not already running).
  </action>
  <verify>
    <automated>cd /Users/mylinh/projects/resume-website && npx playwright test tests/phase2.spec.ts --project=desktop 2>&1 | grep -E "passed|failed" | tail -5</automated>
  </verify>
  <done>User has typed "approved" confirming visual layout matches design intent.</done>
  <what-built>
Complete Phase 2 identity layer:
- Hero section: two-column split with name, title, tagline, circular photo, Contact Me CTA, LinkedIn/GitHub icons, tech marquee
- About section: professional bio (D365/Manhattan DFIO/IBM AS400 keywords), 3 stat callout boxes
- Skills section: 4 liquid-glass category cards in a 2x2 grid with pill chips and emerald hover state
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:4321 in your browser
2. Desktop check (1280px+): Hero shows two columns — text left, photo right. Both LinkedIn and GitHub icons appear below the CTAs.
3. Mobile check (375px — use DevTools responsive mode): Hero stacks with photo on top, text below. No horizontal scroll.
4. Scroll to About: bio paragraph is readable, 3 stat boxes visible on the right (desktop) or below (mobile).
5. Scroll to Skills: 4 cards in a 2x2 grid (desktop) or single column (mobile). Hover over a chip — border and text should turn emerald.
6. Click "Contact Me" CTA — should smoothly scroll to the bottom contact stub section.
7. Click nav "About" link — should scroll to About section content (not a blank area).
  </how-to-verify>
  <resume-signal>Type "approved" if the sections look correct, or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
npx playwright test tests/phase2.spec.ts --project=desktop
npx playwright test tests/phase1.spec.ts --project=desktop
npx playwright test (full suite)
</verification>

<success_criteria>
All 12 Phase 2 smoke tests pass. All Phase 1 tests remain green. User has visually approved hero two-column layout, about section with stats, and skills grid with chip hover states.
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-about-skills/02-05-SUMMARY.md`
</output>
