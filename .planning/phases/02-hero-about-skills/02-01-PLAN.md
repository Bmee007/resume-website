---
phase: 02-hero-about-skills
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase2.spec.ts
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04
  - SKILL-05

must_haves:
  truths:
    - "All 12 Phase 2 requirements have a corresponding failing test before any implementation begins"
    - "tests/phase2.spec.ts runs without TypeScript errors"
    - "Phase 1 tests remain green after adding the new spec file"
  artifacts:
    - path: "tests/phase2.spec.ts"
      provides: "12 smoke tests covering HERO-01 through SKILL-05"
      exports: []
  key_links:
    - from: "tests/phase2.spec.ts"
      to: "dev server at localhost:4321"
      via: "Playwright page.goto('/')"
      pattern: "page\\.goto"
---

<objective>
Create the Phase 2 Playwright smoke test scaffold. All 12 tests must exist and fail RED before any implementation task runs (Nyquist compliance).

Purpose: Establish automated feedback loop for the hero, about, and skills sections. Every implementation task can verify its work in ~15 seconds with a targeted test run.
Output: tests/phase2.spec.ts with 12 failing smoke tests (one per requirement ID).
</objective>

<execution_context>
@/Users/mylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-about-skills/02-CONTEXT.md
@.planning/phases/02-hero-about-skills/02-VALIDATION.md
</context>

<interfaces>
<!-- Existing test pattern from tests/phase1.spec.ts -->
```typescript
import { test, expect } from "@playwright/test";

test("HERO-01 @smoke — ...", async ({ page }) => {
  await page.goto("/");
  // assertions here
});
```

Playwright config: playwright.config.ts — desktop project at 1280x800, base URL http://localhost:4321
Quick run command: npx playwright test tests/phase2.spec.ts --project=desktop
Per-requirement run: npx playwright test tests/phase2.spec.ts -g "HERO-01" --project=desktop
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/phase2.spec.ts with 12 RED smoke tests</name>
  <files>tests/phase2.spec.ts</files>
  <action>
Create tests/phase2.spec.ts following the exact same import pattern as tests/phase1.spec.ts (ESM imports, no __dirname workaround needed since these are page tests not filesystem tests).

Write one test per requirement ID. Each test must FAIL on the current codebase (the sections don't exist yet or lack required content). Tests use the test title prefix "REQ-ID @smoke —" pattern for targeted filtering.

Test implementations:

**HERO-01** — Check hero section contains "Borina Keo" text and "ERP & WMS AI Integration Leader" text both visible in viewport at page load. The H1 already exists in HeroSection.astro so this test should PASS — write it anyway as the green baseline for hero heading.

**HERO-02** — Query `img.hero-photo` (or `[data-testid="hero-photo"]`). Expect the element to be visible. This FAILS until the photo column is added.

**HERO-03** — Click the `a[href="#contact"]` CTA. After click, expect `#contact` to be in viewport. Note: #contact section exists as a stub in index.astro so scroll target exists. This test may pass already — write it and accept green.

**HERO-04** — Expect `a[aria-label="LinkedIn profile"]` to be visible and have href containing "linkedin.com". Expect `a[aria-label="GitHub profile"]` to be visible and have href containing "github.com/Bmee007". Both FAIL until social icons are added.

**ABOUT-01** — Navigate to page, scroll `#about` into view, expect `#about` to be visible and contain text matching `/bio|about|experience/i` (the placeholder stub only says "About — Phase 2"). Alternatively: expect a `.about-bio` element to be visible. This FAILS until AboutSection is implemented.

**ABOUT-02** — Expect `#about` section text to contain "D365" AND "Manhattan DFIO" AND "IBM AS400". FAILS until bio prose is added.

**ABOUT-03** — Expect `.stat-box` (or `[data-testid="stat-box"]`) to have count of 3 (three stat callout elements). FAILS until stat callouts are added.

**SKILL-01** — Scroll `#skills` into view, expect `.skill-card` elements to have count of 4. FAILS until SkillsSection is added.

**SKILL-02** — Expect `#skills` to contain text "Microsoft Dynamics 365" and "IBM AS400". FAILS until SkillsSection is added.

**SKILL-03** — Expect `#skills` to contain text "Manhattan DFIO". FAILS until SkillsSection is added.

**SKILL-04** — Expect `#skills` to contain text "Azure OpenAI" and "Power Automate". FAILS until SkillsSection is added.

**SKILL-05** — Expect `#skills` to contain `.skill-chip` elements (not a plain `ul > li` list). Use `page.locator('#skills .skill-chip').count()` and expect count > 0. Also assert `page.locator('#skills ul li').count()` equals 0 to confirm no plain list. FAILS until SkillsSection is added.

Use `scrollIntoViewIfNeeded()` before assertions on sections below the fold.
  </action>
  <verify>
    <automated>cd /Users/mylinh/projects/resume-website && npx playwright test tests/phase2.spec.ts --project=desktop 2>&1 | tail -20</automated>
  </verify>
  <done>
tests/phase2.spec.ts exists with 12 tests. Running the test command shows test results — most tests fail (RED) because implementation doesn't exist yet. HERO-01 and HERO-03 may be green already (heading exists, contact stub exists). No TypeScript compile errors. Phase 1 tests still pass: `npx playwright test tests/phase1.spec.ts --project=desktop` exits 0.
  </done>
</task>

</tasks>

<verification>
npx playwright test tests/phase2.spec.ts --project=desktop
npx playwright test tests/phase1.spec.ts --project=desktop
</verification>

<success_criteria>
tests/phase2.spec.ts exists with 12 tests matching requirement IDs HERO-01 through SKILL-05. Tests run without compilation errors. Phase 1 tests remain green. At least 8 of the 12 Phase 2 tests are failing (confirming RED baseline for unimplemented sections).
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-about-skills/02-01-SUMMARY.md`
</output>
