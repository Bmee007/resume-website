---
phase: 03-experience-ai-projects
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/components/ExperienceSection.astro
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04
  - EXP-05

must_haves:
  truths:
    - "ExperienceSection.astro renders a vertical single-column timeline with 3 entries"
    - "Two entries (D365 migration, Manhattan DFIO) are visually elevated with liquid-glass surface + emerald left-border stripe + pulsing dot"
    - "Regular entry uses flat slate-800 card with slate-700 border, no glass effect"
    - "Each entry shows job title, company, date range, and 2-3 bullet accomplishments"
    - "Timeline renders without horizontal overflow at 375px (EXP-05 satisfied structurally)"
  artifacts:
    - path: "src/components/ExperienceSection.astro"
      provides: "Experience timeline section component"
      contains: ".timeline-entry, .is-featured, .timeline-track"
  key_links:
    - from: "src/components/ExperienceSection.astro"
      to: ".liquid-glass"
      via: "global class from global.css applied to featured entry cards"
      pattern: "liquid-glass"
    - from: "src/components/ExperienceSection.astro"
      to: ".entry-inner"
      via: "position: relative; z-index: 1 — prevents content hidden behind ::before pseudo-element"
      pattern: "z-index: 1"
---

<objective>
Build ExperienceSection.astro — the career timeline component with 3 experience entries, CSS vertical rule + dots, featured entry elevation (liquid-glass + emerald border + pulsing dot), and flat slate card for the regular entry.

Purpose: Satisfies EXP-01 through EXP-05. Enterprise visitors can verify Borina's quantified track record.
Output: src/components/ExperienceSection.astro (self-contained, scoped styles, zero JS, zero new deps)
</objective>

<execution_context>
@/Users/mylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-experience-ai-projects/03-CONTEXT.md
@.planning/phases/03-experience-ai-projects/03-RESEARCH.md
@.planning/phases/03-experience-ai-projects/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns from existing codebase the executor must follow exactly -->

From src/styles/global.css:
```css
/* Design tokens */
:root {
  --background:   260 87% 3%;
  --foreground:   40 6% 95%;
  --primary:      121 95% 76%;   /* emerald — use for dots */
  --border-hsl:   240 4% 20%;
  --secondary:    240 4% 16%;
}
/* Legacy tokens */
--color-surface: #1E293B;
--color-border:  #334155;
--color-accent:  #10B981;
--glow-accent:   0 0 12px rgba(16, 185, 129, 0.35);

/* .liquid-glass global class: backdrop-filter glass with gradient border via ::before pseudo-element */
/* CRITICAL: .liquid-glass::before uses position:absolute; inset:0; z-index:0 */
/* Any inner content wrapper MUST have position:relative; z-index:1 to render above ::before */
```

From src/components/SkillsSection.astro (section structure to mirror):
```astro
<section id="skills" class="skills-root">
  <div class="skills-container">
    <div class="skills-header">
      <h2>Skills &amp; Expertise</h2>
      <p class="skills-tagline">Subheadline here</p>
    </div>
    <!-- grid content -->
  </div>
</section>

<style>
  .skills-root { padding: 5rem 1.5rem; }
  .skills-container { max-width: 72rem; margin: 0 auto; }
  .skills-header { margin-bottom: 3rem; text-align: center; }
  .skills-header h2 { font-size: clamp(1.75rem, 4vw, 2.75rem); font-weight: 600; color: hsl(var(--foreground)); margin: 0; }
  .skills-tagline { font-size: 1.125rem; color: hsl(var(--foreground) / 0.6); margin-top: 0.5rem; }
</style>
```

Established Phase 2 pattern — inner wrapper z-index:
```astro
<!-- AboutSection.astro / SkillsSection.astro pattern -->
<div class="liquid-glass skill-card rounded-2xl">
  <div class="skill-card-inner">  <!-- position:relative; z-index:1 in scoped style -->
    content here
  </div>
</div>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build ExperienceSection.astro with vertical timeline</name>
  <files>src/components/ExperienceSection.astro</files>
  <action>
Create src/components/ExperienceSection.astro as a fully self-contained Astro component.

**Frontmatter data array (3 entries, chronological descending):**

```javascript
const experiences = [
  {
    title: "AI & ERP Integration Lead",
    company: "[Current Company]",
    dateRange: "2023 – Present",
    featured: false,
    bullets: [
      "Architected Power Automate + AI Builder workflows, automating [X] manual processes across finance and ops",
      "Led Azure OpenAI pilot for supply chain demand forecasting, achieving [Y]% forecast accuracy improvement",
    ],
  },
  {
    title: "ERP Implementation Lead — D365 Migration",
    company: "[Company B]",
    dateRange: "2019 – 2023",
    featured: true,
    bullets: [
      "Directed full Microsoft Dynamics 365 migration from IBM AS400, cutting operational costs by [X]%",
      "Integrated Azure OpenAI into D365 procurement workflows, reducing cycle time by [Y]%",
      "Led [Z]-person cross-functional team through 18-month ERP modernization program",
    ],
  },
  {
    title: "WMS Implementation Lead — Manhattan DFIO",
    company: "[Company C]",
    dateRange: "2016 – 2019",
    featured: true,
    bullets: [
      "Deployed Manhattan DFIO across [X] distribution centers, increasing warehouse throughput by [Y]%",
      "Integrated AI-driven pick optimization into Manhattan DFIO, reducing labor costs by [Z]%",
      "Partnered with C-suite stakeholders to align WMS roadmap with 5-year supply chain strategy",
    ],
  },
];
```

**Template structure:**
- Section `id="experience"` wrapping the component
- Section header: h2 "Experience" + p.exp-tagline "Quantified impact across ERP, WMS, and AI transformation"
- `.timeline-track` div wrapping all entries (provides the vertical ::before rule)
- Each entry: `<div class:list={["timeline-entry", entry.featured && "is-featured"]}>`
  - Inside: `<div class:list={["entry-card rounded-2xl", entry.featured ? "liquid-glass" : "entry-card--flat"]}>`
    - Inside: `<div class="entry-inner">` (z-index:1 in scoped style)
      - `.entry-header`: h3 job title + span.entry-company + span.entry-dates
      - `<ul class="entry-bullets">` with `<li>` for each bullet

**Scoped CSS rules to include:**

Section shell — mirrors SkillsSection padding/container pattern:
```css
.exp-root { padding: 5rem 1.5rem; }
.exp-container { max-width: 72rem; margin: 0 auto; }
.exp-header { margin-bottom: 3rem; text-align: center; }
.exp-header h2 { font-size: clamp(1.75rem, 4vw, 2.75rem); font-weight: 600; color: hsl(var(--foreground)); margin: 0; }
.exp-tagline { font-size: 1.125rem; color: hsl(var(--foreground) / 0.6); margin-top: 0.5rem; margin-bottom: 0; }
```

Timeline track and dots:
```css
.timeline-track {
  position: relative;
  padding-left: 2.5rem;
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}
/* Vertical connecting line */
.timeline-track::before {
  content: '';
  position: absolute;
  left: 0.5rem;
  top: 1.25rem;
  bottom: 1.25rem;
  width: 2px;
  background: hsl(var(--border-hsl));
}
/* Entry dot */
.timeline-entry {
  position: relative;
}
.timeline-entry::before {
  content: '';
  position: absolute;
  left: -2.125rem;
  top: 1.125rem;
  width: 0.625rem;
  height: 0.625rem;
  border-radius: 9999px;
  background: hsl(var(--primary));
  z-index: 1;
}
/* Pulse animation on featured entry dot */
.timeline-entry.is-featured::before {
  box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.5);
  animation: dot-pulse 2.5s ease-in-out infinite;
}
@keyframes dot-pulse {
  0%   { box-shadow: 0 0 0 0   rgba(16, 185, 129, 0.5); }
  50%  { box-shadow: 0 0 0 6px rgba(16, 185, 129, 0); }
  100% { box-shadow: 0 0 0 0   rgba(16, 185, 129, 0); }
}
```

Entry card surfaces:
```css
/* Flat card for regular entries */
.entry-card--flat {
  background: #1E293B;
  border: 1px solid #334155;
  border-radius: 1rem;
}
/* Emerald left-border stripe for featured entries */
.is-featured .entry-card {
  border-left: 3px solid #10B981;
}
```

Inner wrapper (CRITICAL — z-index:1 prevents content hidden behind liquid-glass::before):
```css
.entry-inner {
  position: relative;
  z-index: 1;
  padding: 1.75rem;
}
```

Entry header and bullets:
```css
.entry-header { margin-bottom: 1rem; }
.entry-title { font-size: 1.125rem; font-weight: 600; color: hsl(var(--foreground)); margin: 0 0 0.25rem; }
.entry-meta { display: flex; flex-wrap: wrap; gap: 0.5rem 1rem; align-items: center; }
.entry-company { font-size: 0.9375rem; color: hsl(var(--foreground) / 0.7); }
.entry-dates { font-size: 0.875rem; color: hsl(var(--foreground) / 0.5); }
.entry-bullets { margin: 0; padding-left: 1.25rem; display: flex; flex-direction: column; gap: 0.5rem; }
.entry-bullets li { font-size: 0.9375rem; color: hsl(var(--foreground) / 0.8); line-height: 1.6; }
```

IMPORTANT implementation notes:
- Use `class:list` directive (NOT string concatenation) for conditional classes on entry divs
- The `timeline-entry` div needs `position: relative` for the `::before` dot positioning (included in the CSS above)
- Max width of the timeline track can match the skills-container width (72rem) for consistent layout
- Do NOT add `overflow: hidden` to `.timeline-track` — it would clip the dot that sits outside the padding area
  </action>
  <verify>
    <automated>cd /Users/mylinh/projects/resume-website && npx playwright test tests/phase3.spec.ts -g "EXP-" --project=desktop 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 EXP-* smoke tests pass (EXP-01 through EXP-05). Component file exists at src/components/ExperienceSection.astro. No TypeScript or Astro build errors.</done>
</task>

</tasks>

<verification>
Run `npx playwright test tests/phase3.spec.ts -g "EXP-" --project=desktop && npx playwright test tests/phase3.spec.ts -g "EXP-05" --project=mobile` — all 5 EXP-* tests pass on both desktop and mobile projects.
</verification>

<success_criteria>
ExperienceSection.astro created and all 5 EXP-* Playwright smoke tests pass. The component uses liquid-glass for featured entries, flat slate-800 for regular entries, CSS-only pulsing dot animation on featured entries, and no horizontal overflow at 375px.
</success_criteria>

<output>
After completion, create `.planning/phases/03-experience-ai-projects/03-02-SUMMARY.md`
</output>
