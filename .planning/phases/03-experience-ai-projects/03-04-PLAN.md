---
phase: 03-experience-ai-projects
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - src/pages/index.astro
autonomous: false
requirements:
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04
  - EXP-05
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04

must_haves:
  truths:
    - "index.astro imports and renders ExperienceSection and ProjectsSection replacing the placeholder stubs"
    - "All 9 Phase 3 smoke tests pass (green) on desktop and mobile"
    - "The live dev server shows the experience timeline and AI projects sections visually"
    - "No other existing sections (Hero, About, Skills) are broken"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Fully wired single-page layout with ExperienceSection and ProjectsSection"
      contains: "ExperienceSection, ProjectsSection"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/ExperienceSection.astro"
      via: "import ExperienceSection from '../components/ExperienceSection.astro'"
      pattern: "ExperienceSection"
    - from: "src/pages/index.astro"
      to: "src/components/ProjectsSection.astro"
      via: "import ProjectsSection from '../components/ProjectsSection.astro'"
      pattern: "ProjectsSection"
---

<objective>
Wire ExperienceSection and ProjectsSection into index.astro by replacing the placeholder section stubs, then run the full Phase 3 smoke test suite and perform a visual checkpoint.

Purpose: Connects the two built components to the page, completing Phase 3. Satisfies all 9 requirements EXP-01 through PROJ-04 end-to-end.
Output: Updated src/pages/index.astro with both components imported and rendered; all 9 smoke tests green.
</objective>

<execution_context>
@/Users/mylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-experience-ai-projects/03-CONTEXT.md
@.planning/phases/03-experience-ai-projects/03-02-SUMMARY.md
@.planning/phases/03-experience-ai-projects/03-03-SUMMARY.md
</context>

<interfaces>
<!-- index.astro current stub structure (from codebase inspection) -->
<!--
Line 15: <section id="experience" class="min-h-screen flex items-center justify-center">
  [placeholder content — "Experience — Phase 3"]
</section>

Line 19: <section id="projects" class="min-h-screen flex items-center justify-center">
  [placeholder content — "Projects — Phase 3"]
</section>

The import block at the top of index.astro's frontmatter already imports:
  import HeroSection from "../components/HeroSection.astro";
  import AboutSection from "../components/AboutSection.astro";
  import SkillsSection from "../components/SkillsSection.astro";

Add alongside these:
  import ExperienceSection from "../components/ExperienceSection.astro";
  import ProjectsSection from "../components/ProjectsSection.astro";

Replace section stubs with:
  <ExperienceSection />
  <ProjectsSection />
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire components into index.astro and run full smoke suite</name>
  <files>src/pages/index.astro</files>
  <action>
Edit src/pages/index.astro to:

1. In the frontmatter `---` block, add two import lines alongside the existing component imports:
   ```
   import ExperienceSection from "../components/ExperienceSection.astro";
   import ProjectsSection from "../components/ProjectsSection.astro";
   ```

2. Find the placeholder `<section id="experience" ...>` stub (contains "Experience" placeholder text and flex centering classes) — replace the ENTIRE element (opening tag through closing `</section>`) with:
   ```
   <ExperienceSection />
   ```

3. Find the placeholder `<section id="projects" ...>` stub (contains "Projects" placeholder text) — replace the ENTIRE element with:
   ```
   <ProjectsSection />
   ```

4. Run the dev server build check:
   ```bash
   cd /Users/mylinh/projects/resume-website && npm run build 2>&1 | tail -20
   ```
   Fix any import path or build error before proceeding.

5. Run the full Phase 3 smoke suite against the dev server:
   ```bash
   cd /Users/mylinh/projects/resume-website && npx playwright test tests/phase3.spec.ts --project=desktop --project=mobile 2>&1 | tail -30
   ```
   All 9 tests must pass. If any fail, read the error, fix the component or wiring, re-run.

6. Also run the existing phase 1 and phase 2 smoke tests to confirm no regressions:
   ```bash
   cd /Users/mylinh/projects/resume-website && npx playwright test tests/phase1.spec.ts tests/phase2.spec.ts --project=desktop 2>&1 | tail -20
   ```
  </action>
  <verify>
    <automated>cd /Users/mylinh/projects/resume-website && npx playwright test tests/phase3.spec.ts --project=desktop --project=mobile 2>&1 | tail -20</automated>
  </verify>
  <done>All 9 Phase 3 smoke tests pass on desktop and mobile. Phase 1 and Phase 2 smoke tests still pass (no regressions). index.astro no longer contains placeholder stub sections for experience or projects.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    ExperienceSection (vertical timeline with 3 entries, pulsing emerald dots on featured entries, liquid-glass elevated cards for D365 and Manhattan DFIO roles, flat slate card for current role) and ProjectsSection (3 liquid-glass project cards in 2-column grid, system/AI tech chips, GitHub links on D365 and Manhattan DFIO project cards) — both wired into the live page.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` in the project directory and open http://localhost:4321 in a browser
    2. Scroll to the Experience section — verify:
       - Three stacked entries visible (current role at top, D365 migration second, Manhattan DFIO third)
       - A vertical emerald line runs down the left connecting all entries
       - D365 and Manhattan DFIO entries have emerald left-border stripe + elevated glass surface
       - Featured entry dots pulse subtly (slow emerald glow animation)
       - Current role entry uses a flat darker card (no glass effect)
    3. Scroll to the AI Integrations section — verify:
       - Heading reads "AI Integrations"
       - Subheadline reads "Leading AI-powered transformation across ERP & WMS systems"
       - Three cards render in 2-column layout (desktop) or single column (narrow window)
       - Each card shows system chip + AI tech chip + outcome text
       - "View on GitHub" link appears on D365 and Manhattan DFIO project cards
    4. Resize to 375px width — verify the timeline entries stack cleanly with no horizontal overflow, and project cards go single column
    5. Confirm Hero, About, and Skills sections above still look correct (no regressions)
  </how-to-verify>
  <resume-signal>Type "approved" if the sections look correct, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
Full Phase 3 suite green: `npx playwright test tests/phase3.spec.ts --project=desktop --project=mobile`
No regressions: `npx playwright test tests/phase1.spec.ts tests/phase2.spec.ts --project=desktop`
Visual checkpoint approved by user.
</verification>

<success_criteria>
index.astro imports and renders ExperienceSection and ProjectsSection. All 9 Phase 3 smoke tests pass on desktop and mobile. Phase 1/2 tests still pass. User approves visual appearance at the checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/03-experience-ai-projects/03-04-SUMMARY.md`
</output>
