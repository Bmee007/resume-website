---
phase: 03-experience-ai-projects
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase3.spec.ts
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04
  - EXP-05
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04

must_haves:
  truths:
    - "tests/phase3.spec.ts exists and contains 9 failing smoke tests (one per requirement EXP-01 through PROJ-04)"
    - "All 9 tests fail RED before ExperienceSection or ProjectsSection are built"
    - "Test locators scope to #experience and #projects section IDs to prevent strict mode violations"
  artifacts:
    - path: "tests/phase3.spec.ts"
      provides: "Playwright smoke test suite for Phase 3 requirements"
      exports: []
  key_links:
    - from: "tests/phase3.spec.ts"
      to: "#experience .timeline-entry"
      via: "page.locator('#experience .timeline-entry')"
      pattern: "#experience .timeline-entry"
    - from: "tests/phase3.spec.ts"
      to: "#projects .project-card"
      via: "page.locator('#projects .project-card')"
      pattern: "#projects .project-card"
---

<objective>
Create the Wave 0 Playwright smoke test scaffold for Phase 3 — 9 RED tests covering every requirement EXP-01 through PROJ-04. All tests must fail before components are built. They turn GREEN when ExperienceSection and ProjectsSection are implemented and wired.

Purpose: Establish measurable acceptance criteria before implementation begins. RED tests define what "done" means for each requirement.
Output: tests/phase3.spec.ts with 9 @smoke-tagged test cases, all failing.
</objective>

<execution_context>
@/Users/mylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-experience-ai-projects/03-CONTEXT.md
@.planning/phases/03-experience-ai-projects/03-RESEARCH.md
</context>

<interfaces>
<!-- Existing test patterns from phase2.spec.ts — follow exactly -->
<!--
Pattern: scope ALL locators to section ID to prevent Playwright strict mode violations
Pattern: use scrollIntoViewIfNeeded() before assertions
Pattern: use @smoke tag on every test title
Pattern: use --project=desktop for most tests; use --project=mobile for EXP-05

From tests/phase2.spec.ts (established pattern):
  test("HERO-01 @smoke — hero section visible with name and title", async ({ page }) => {
    await page.goto("/");
    const hero = page.locator("#hero");
    await hero.scrollIntoViewIfNeeded();
    await expect(hero).toBeVisible();
  });

From playwright.config.ts:
  projects: [
    { name: "desktop", use: { viewport: { width: 1280, height: 720 } } },
    { name: "mobile",  use: { viewport: { width: 375,  height: 812 } } },
  ]

Class names to assert on (established by plan 03-02 and 03-03):
  - .timeline-entry     (each experience entry)
  - .is-featured        (featured entry modifier — D365, Manhattan DFIO)
  - .project-card       (each AI project card)
  - .project-chip       (system/AI-tech chip in project card)
  - .project-outcome    (outcome text in project card)
  - .project-github     (GitHub anchor on relevant cards)
-->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write 9 RED Playwright smoke tests for Phase 3</name>
  <files>tests/phase3.spec.ts</files>
  <behavior>
    - EXP-01: #experience section is visible and contains exactly 3 .timeline-entry elements
    - EXP-02: First .timeline-entry inside #experience contains text for company, title, date range, and at least one bullet list item
    - EXP-03: At least one .timeline-entry inside #experience has class .is-featured and contains text matching /D365|Dynamics 365/i
    - EXP-04: At least one .timeline-entry inside #experience has class .is-featured and contains text matching /Manhattan DFIO/i
    - EXP-05: At 375px viewport, #experience section has no horizontal overflow (scrollWidth equals offsetWidth)
    - PROJ-01: #projects section is visible and contains between 2 and 4 .project-card elements
    - PROJ-02: Each .project-card inside #projects contains at least one .project-chip element and a .project-outcome element
    - PROJ-03: At least one anchor inside #projects links to github.com/Bmee007
    - PROJ-04: The #projects section contains text matching /lead|leading/i (leadership framing — present in the tagline p, not the h2)
  </behavior>
  <action>
Create tests/phase3.spec.ts following the exact pattern from tests/phase2.spec.ts:

1. Import `test, expect` from `@playwright/test`
2. Add a `test.beforeEach` that calls `page.goto("/")`
3. Write one test per requirement, each titled with the requirement ID and @smoke tag, e.g. `"EXP-01 @smoke — experience section visible with 3 timeline entries"`
4. CRITICAL: Scope every locator to the section ID to prevent strict mode violations:
   - `page.locator('#experience .timeline-entry')` — NOT `page.locator('.timeline-entry')`
   - `page.locator('#projects .project-card')` — NOT `page.locator('.project-card')`
5. For EXP-05 mobile test: use `page.setViewportSize({ width: 375, height: 812 })` at the start of the test, then evaluate `document.querySelector('#experience').scrollWidth === document.querySelector('#experience').offsetWidth` via `page.evaluate()`, assert it is true
6. For PROJ-03 GitHub link: `expect(page.locator('#projects a[href*="github.com/Bmee007"]')).toBeVisible()`
7. For PROJ-04 leadership framing: assert the #projects section contains text matching /[Ll]ead/i — scope to the section, NOT the h2, because "Leading..." appears in the tagline <p> element, not the heading: `await expect(page.locator('#projects')).toContainText(/[Ll]ead/i)`
8. For EXP-01 entry count: `await expect(page.locator('#experience .timeline-entry')).toHaveCount(3)`
9. For EXP-02 entry content: locator the first .timeline-entry, call scrollIntoViewIfNeeded(), then assert it contains a visible `<ul>` or `<li>` descendant and visible date-range text (e.g. text matching /\d{4}/)
10. For EXP-03/EXP-04 featured entries: `page.locator('#experience .timeline-entry.is-featured')` then assert inner text matches the system name

Run the tests after writing — they MUST all fail (RED) since the components don't exist yet. If any test unexpectedly passes, investigate why before proceeding.

Do NOT attempt to make tests pass — the goal is confirmed RED state.
  </action>
  <verify>
    <automated>cd /Users/mylinh/projects/resume-website && npx playwright test tests/phase3.spec.ts --project=desktop 2>&1 | tail -20</automated>
  </verify>
  <done>tests/phase3.spec.ts exists with 9 tests; running the suite confirms all 9 fail (expected RED state — components not yet built). Zero syntax errors in the file.</done>
</task>

</tasks>

<verification>
Run `npx playwright test tests/phase3.spec.ts --project=desktop` — all 9 tests fail with "locator resolved to 0 elements" or similar, confirming the stubs have no component content yet.
</verification>

<success_criteria>
tests/phase3.spec.ts committed with 9 @smoke tests; `npx playwright test tests/phase3.spec.ts --project=desktop` exits non-zero with 9 failures. Zero TypeScript/import errors.
</success_criteria>

<output>
After completion, create `.planning/phases/03-experience-ai-projects/03-01-SUMMARY.md`
</output>
