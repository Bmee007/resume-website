---
phase: 05-seo-polish-launch
task: ad-hoc/brainstorming
total_tasks: ad-hoc
status: in_progress
last_updated: "2026-03-15T22:50:13.819Z"
---

<current_state>
Brainstorming complete for two UI changes. Design spec written, reviewed, and approved. Ready to create implementation plan via writing-plans skill, then execute.
</current_state>

<completed_work>

- Visual companion brainstorming session completed
- User selected: Contact section → Option C (frosted glass card)
- User selected: Circuit animation → Option B (lightning bursts)
- Design spec written to: docs/superpowers/specs/2026-03-15-contact-glass-circuit-animation-design.md
- Spec reviewed by code-reviewer agent (10 issues found, all fixed)
- Updated spec committed (6ea5a09)
</completed_work>

<remaining_work>

1. Invoke writing-plans skill to create implementation plan from the spec
2. Execute the plan:
   - ContactSection.astro: add .contact-glass wrapper div + CSS
   - ProjectsSection.astro: replace section-bg div with animated <canvas>, rewrite script block
3. Verify in browser (localhost:4322):
   - Contact section text readable through frosted glass
   - Lightning bursts animate between circuit nodes
   - Static fallback works with prefers-reduced-motion
4. Run npm test to confirm no Playwright regressions
5. Deploy to Vercel
</remaining_work>

<decisions_made>

- Contact fix: frosted glass card wrapping heading+tagline+form+social (footer stays outside)
  background: rgba(10,15,26,0.75), backdrop-filter: blur(12px), emerald border
- Circuit animation: lightning bursts — node flashes, glowing beam to neighbor, dest glow on arrival
  600ms interval, 35-frame duration, requestAnimationFrame loop
- prefers-reduced-motion: static canvas only (no rAF, no setInterval)
- DPR handling: multiply canvas dims by devicePixelRatio, ctx.scale(dpr,dpr) after resize
- Topology: 29 nodes, 39 edges — normalized coords in spec (converted from 1200x300 SVG viewBox)
- IntersectionObserver: replaces old projectsBgObserver, targets #circuit-canvas
- Tab visibility: cancelAnimationFrame on hidden, restart on visible
</decisions_made>

<blockers>

- Context window was at ~78% when pausing — fresh session required before implementation
- Visual companion server running on port 56993 (may need restart in new session)
</blockers>

<context>
The design is fully specced and ready to implement. The spec is detailed enough to implement directly — it includes exact CSS, JS code snippets, normalized node coordinates, and edge list. The writing-plans skill should produce a 2-task plan (one task per component). Implementation should be straightforward with the spec as a reference.
</context>

<next_action>
Start with: invoke writing-plans skill pointing at the design spec at docs/superpowers/specs/2026-03-15-contact-glass-circuit-animation-design.md, then execute the resulting plan.
</next_action>
