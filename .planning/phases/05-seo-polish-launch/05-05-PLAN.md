---
phase: 05-seo-polish-launch
plan: 05
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
  - 05-04
files_modified:
  - src/pages/404.astro
  - public/og-image.png
  - src/pages/index.astro
autonomous: false
requirements:
  - POLSH-05
  - SEO-03
  - SEO-04
  - SEO-05
  - POLSH-04

user_setup:
  - service: og-image
    why: "OG image is a static PNG created with a design tool — requires human action to create the file"
    env_vars: []
    dashboard_config:
      - task: "Create public/og-image.png (1200x630px)"
        location: "Figma, Canva, or any image editor — or provide a pre-existing PNG"

must_haves:
  truths:
    - "Visiting /nonexistent-path renders a styled 404 page with the site nav and a home CTA link"
    - "All img elements across all components have non-empty alt text"
    - "public/og-image.png exists at 1200x630 with dark navy background and Borina's name in white"
    - "Lighthouse mobile score is 90+ on the live Vercel URL (verified manually)"
    - "No WCAG AA contrast failures on dark background text (verified manually with axe DevTools)"
  artifacts:
    - path: "src/pages/404.astro"
      provides: "Styled 404 page with Nav, witty ERP-themed headline, home CTA button"
      contains: "BaseLayout"
    - path: "public/og-image.png"
      provides: "1200x630 OG image for LinkedIn/Twitter card previews"
  key_links:
    - from: "src/pages/404.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "import BaseLayout (which includes Nav automatically)"
      pattern: "import BaseLayout"
    - from: "src/layouts/BaseLayout.astro og:image meta tag"
      to: "public/og-image.png"
      via: "og:image content pointing to /og-image.png"
      pattern: "og-image\\.png"
---

<objective>
Create the styled 404 page, audit all image alt text (SEO-05), and provide the OG image. End with human verification of Lighthouse score and WCAG contrast.

Purpose: Complete the final polish layer — 404 page gives lost visitors a branded exit ramp, alt text satisfies accessibility/SEO requirements, and the Lighthouse/WCAG checkpoint confirms the site is launch-ready.
Output: src/pages/404.astro; verified alt text across all components; human-confirmed Lighthouse 90+ and WCAG AA pass.
</objective>

<execution_context>
@/Users/mylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-seo-polish-launch/05-CONTEXT.md
@.planning/phases/05-seo-polish-launch/05-RESEARCH.md
@.planning/phases/05-seo-polish-launch/05-02-SUMMARY.md
@.planning/phases/05-seo-polish-launch/05-03-SUMMARY.md
@.planning/phases/05-seo-polish-launch/05-04-SUMMARY.md
</context>

<interfaces>
<!-- BaseLayout.astro interface after Plan 05-02 -->
```astro
interface Props {
  title: string;
  description?: string;
  ogTitle?: string;
  ogDescription?: string;
  ogImage?: string;
  canonicalUrl?: string;
}
// Nav is rendered inside BaseLayout body automatically
// Full SEO head is included
```

<!-- 404.astro pattern -->
```astro
---
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout title="Page Not Found — Borina Keo" description="This page doesn't exist.">
  <main class="error-page">
    <h1>Even ERP migrations hit a wrong path.</h1>
    <p>The page you're looking for isn't here — but the right path is just a click away.</p>
    <a href="/" class="btn-hero rounded-full">Back to Home</a>
  </main>
</BaseLayout>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create styled 404 page and audit all image alt text</name>
  <files>src/pages/404.astro, src/pages/index.astro</files>
  <action>
    CREATE src/pages/404.astro:
    Import BaseLayout (which auto-includes Nav). Use the ERP-themed witty line from CONTEXT.md as the starting reference: "Even ERP migrations hit a wrong path." Refine the copy slightly within the "brief + professional" constraint:

    Suggested final copy (Claude's discretion within the constraint):
    - Headline: "Even ERP migrations hit a wrong path."
    - Sub: "This page doesn't exist — but your next integration opportunity does."
    - CTA button: "Back to Home" (href="/")

    Style using scoped CSS matching the site:
    - Background: transparent (BaseLayout provides dark navy body background)
    - min-height: calc(100svh - 5rem) to fill viewport below nav
    - Center the content vertically with flexbox: `display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; gap: 1.5rem; padding: 4rem 1.5rem;`
    - Headline: use `color: hsl(var(--foreground))`, font-size clamp(1.75rem, 5vw, 3rem), font-weight 600
    - Sub: `color: hsl(var(--foreground) / 0.7)`, font-size 1.125rem
    - CTA button: reuse `.btn-hero` class (global) with `.rounded-full`

    Do NOT import Nav separately — BaseLayout already includes it. Do NOT use emerald (#10B981) as body text color — contrast failure risk on normal weight text.

    ALT TEXT AUDIT (SEO-05):
    Read the following components and identify all `<img>` tags:
    - src/components/HeroSection.astro (borina-photo.jpg — check alt is non-empty)
    - src/pages/index.astro (if any img tags)
    - All brand-icon `<img>` tags in the HeroSection marquee

    For brand icons in the marquee: they already use `alt={tech.name}` — confirm this is correct.
    For the hero photo: alt is "Borina Keo — ERP &amp; WMS AI Integration Leader" — confirm non-empty.

    Fix any img tag found with empty alt="" or missing alt attribute.

    Test 404: Run `npm run dev`, visit http://localhost:4321/this-page-does-not-exist — confirm the custom 404 page renders (not Astro's default "404 Not Found" text page). If Astro dev server serves its own 404 in dev mode, this is expected — the real test is after build+preview: `npm run build && npm run preview` then visit http://localhost:4321/this-page-does-not-exist.
  </action>
  <verify>
    <automated>npx playwright test tests/phase5.spec.ts --grep "POLSH-05|SEO-05" --project=chromium 2>&1 | tail -15</automated>
  </verify>
  <done>src/pages/404.astro exists, renders with Nav + witty ERP headline + home CTA. All img elements have non-empty alt text. POLSH-05 and SEO-05 smoke tests pass GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 5 implementation:
    - SEO head in BaseLayout (OG tags, JSON-LD, canonical, keyword title/description)
    - @astrojs/sitemap integration generating sitemap-index.xml
    - public/robots.txt with Sitemap directive
    - Hero load animation (CSS keyframes, no JS)
    - Scroll-triggered section animations (IntersectionObserver)
    - Lift+glow hover states on all cards and social links
    - Styled 404 page with ERP-themed copy
    - All image alt text verified
  </what-built>
  <how-to-verify>
    **Step 1 — Full smoke test suite:**
    ```
    npx playwright test tests/phase5.spec.ts --project=chromium
    ```
    Expected: All 9 automated tests pass GREEN (SEO-04 and POLSH-04 are manual-only — skip).

    **Step 2 — OG Image (action required):**
    Create public/og-image.png (1200x630px):
    - Background: #0F172A dark navy
    - Top: "Borina Keo" in white, ~72px, weight 600
    - Below: "ERP & WMS AI Integration Leader" in #10B981 emerald, ~36px
    - Optional: thin emerald left-edge stripe for brand accent

    Use Figma, Canva, or any image editor. Place at public/og-image.png.

    **Step 3 — Lighthouse mobile score:**
    1. Run `npm run build && npm run preview` (or use the live Vercel URL)
    2. Open Chrome DevTools → Lighthouse → Mobile → Performance
    3. Run the audit
    Expected: Performance score 90 or above.
    If score is below 90 and Lighthouse points to the hero-video.mp4 as LCP element, add `preload="none"` to the `<video>` tag in HeroSection.astro.

    **Step 4 — WCAG AA contrast check:**
    1. Run `npm run dev`, open http://localhost:4321
    2. Install axe DevTools Chrome extension (free) or run Lighthouse Accessibility audit
    3. Run the accessibility check
    Expected: No contrast failures on body text. If emerald (#10B981) is flagged as a text color on small text, change those elements to use `hsl(var(--foreground))` instead.

    **Step 5 — LinkedIn OG card preview:**
    Share the live Vercel URL in the LinkedIn post composer (you can draft without publishing)
    Expected: Card renders with dark navy background, "Borina Keo" name, and the og-image.png you created.

    **Step 6 — 404 page:**
    Visit https://[your-vercel-url]/this-page-does-not-exist
    Expected: Custom 404 page with Nav bar visible and "Back to Home" button.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass, or describe any issues found (e.g., "Lighthouse score is 87 — video flagged", "axe found contrast failure on .chip-label").
  </resume-signal>
</task>

</tasks>

<verification>
After checkpoint approval:
npx playwright test --project=chromium
Expected: Full test suite passes (all phases).
</verification>

<success_criteria>
src/pages/404.astro exists and renders correctly. All img alt text is non-empty. public/og-image.png placed at correct path. Lighthouse mobile score 90+ confirmed by human. WCAG AA passes. All 9 Phase 5 automated smoke tests GREEN.
</success_criteria>

<output>
After completion, create .planning/phases/05-seo-polish-launch/05-05-SUMMARY.md
</output>
