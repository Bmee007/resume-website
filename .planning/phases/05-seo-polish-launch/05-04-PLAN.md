---
phase: 05-seo-polish-launch
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/SkillsSection.astro
  - src/components/ExperienceSection.astro
  - src/components/ProjectsSection.astro
  - src/components/HeroSection.astro
  - src/components/ContactSection.astro
autonomous: true
requirements:
  - POLSH-02
  - POLSH-03

must_haves:
  truths:
    - "Skill cards lift 4px and emit emerald glow on hover"
    - "Project cards lift 4px and emit emerald glow on hover (translateY added to existing glow)"
    - "Experience timeline entry cards lift 4px and emit emerald glow on hover"
    - "Social icon links in Hero and Contact sections turn emerald on hover"
    - "Emerald #10B981 accent appears on at least 3 distinct element types across the page"
    - "Hover transitions are 150-200ms — snappy and immediate"
  artifacts:
    - path: "src/components/SkillsSection.astro"
      provides: ".skill-card hover: translateY(-4px) + box-shadow glow + transition"
      contains: "translateY(-4px)"
    - path: "src/components/ExperienceSection.astro"
      provides: ".entry-inner or .entry-card hover: translateY(-4px) + box-shadow glow + transition"
      contains: "translateY(-4px)"
    - path: "src/components/ProjectsSection.astro"
      provides: ".project-card hover: translateY(-4px) added (glow already exists)"
      contains: "translateY(-4px)"
  key_links:
    - from: "src/components/SkillsSection.astro"
      to: ".skill-card CSS"
      via: "scoped style :hover selector with transform and box-shadow"
      pattern: "skill-card:hover"
    - from: "src/components/ExperienceSection.astro"
      to: ".entry-inner CSS"
      via: "scoped style :hover selector"
      pattern: "entry.*hover"
---

<objective>
Add lift+glow hover states to all interactive card elements (skill cards, project cards, experience timeline entries) and verify emerald accent consistency across the site.

Purpose: Make interactive elements feel responsive and professional — the lift+glow pattern communicates clickability and reinforces the emerald brand accent throughout.
Output: Updated scoped styles in SkillsSection, ExperienceSection, ProjectsSection; verified social icon hover in HeroSection and ContactSection.
</objective>

<execution_context>
@/Users/mylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-seo-polish-launch/05-CONTEXT.md
@.planning/phases/05-seo-polish-launch/05-RESEARCH.md
@src/components/SkillsSection.astro
@src/components/ExperienceSection.astro
@src/components/ProjectsSection.astro
@src/components/HeroSection.astro
@src/components/ContactSection.astro
</context>

<interfaces>
<!-- Hover pattern contract — apply per component in scoped <style> -->
```css
/* Card lift + glow hover pattern (same for skill-card, project-card, entry-inner) */
.card-class {
  transition: transform 200ms ease, box-shadow 200ms ease;
}
.card-class:hover {
  transform: translateY(-4px);
  box-shadow: 0 0 12px rgba(16, 185, 129, 0.35); /* = var(--glow-accent) */
}
```

<!-- Social icon hover (already in HeroSection — verify, add to ContactSection if missing) -->
```css
.social-icon-link {
  color: hsl(var(--foreground) / 0.4);
  transition: color 200ms ease;
}
.social-icon-link:hover {
  color: #10B981;
}
```

<!-- ProjectsSection note from research: -->
<!-- .project-card:hover already has box-shadow glow but is MISSING translateY(-4px) and transition property -->
<!-- Add both: transition on base rule + transform: translateY(-4px) on :hover -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add lift+glow hover to SkillsSection and ExperienceSection cards</name>
  <files>src/components/SkillsSection.astro, src/components/ExperienceSection.astro</files>
  <action>
    SKILLSSECTION.ASTRO:
    Find the `.skill-card` CSS rule in the scoped `<style>` block. Add `transition: transform 200ms ease, box-shadow 200ms ease;` to the base `.skill-card` rule. Add a new `.skill-card:hover` rule with:
    ```css
    .skill-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 0 12px rgba(16, 185, 129, 0.35);
    }
    ```
    Note: .skill-card already uses `liquid-glass` and has `z-index:1` on `.skill-card-inner` for content. The hover transform applies to the outer `.skill-card` — this is safe because the inner content already has z-index:1 protecting it from the pseudo-element.

    EXPERIENCESECTION.ASTRO:
    Find the card/entry element class. Based on prior phases this is `.entry-inner` (which has the liquid-glass effect) or `.timeline-entry`. Check the component to confirm the actual class name. Add transition to the base rule, add :hover with translateY(-4px) + box-shadow glow.

    Do NOT apply translateY hover to the `.timeline-dot` or `.timeline-track` — only the card body. Do NOT apply to `.entry-company` or `.entry-dates` text elements — only the card container.

    After changes, run `npm run dev` and manually verify hovering a skill card and timeline entry produces the lift+glow effect.
  </action>
  <verify>
    <automated>npx playwright test tests/phase5.spec.ts --grep "POLSH-02" --project=chromium 2>&1 | tail -10</automated>
  </verify>
  <done>SkillsSection skill cards and ExperienceSection entry cards both show translateY(-4px) + emerald glow on hover with 200ms transition. POLSH-02 smoke test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ProjectsSection hover (add translateY), verify social icon hover in Contact, accent audit</name>
  <files>src/components/ProjectsSection.astro, src/components/ContactSection.astro</files>
  <action>
    PROJECTSSECTION.ASTRO:
    Research confirms `.project-card:hover` already has `box-shadow: 0 0 12px rgba(16, 185, 129, 0.35)` but is missing `transform: translateY(-4px)` and the `transition` property on the base rule.

    Find the `.project-card` rule and add: `transition: transform 200ms ease, box-shadow 200ms ease;`
    Find the `.project-card:hover` rule and add: `transform: translateY(-4px);`

    CONTACTSECTION.ASTRO:
    Locate the `.social-icon-link` styles. Per research, HeroSection has `color: #10B981` on `.social-icon-link:hover` but ContactSection re-declares its own local `.social-icon-link` (Astro scoped styles don't share). Verify the ContactSection scoped styles include:
    ```css
    .social-icon-link {
      color: hsl(var(--foreground) / 0.4);
      transition: color 200ms ease;
    }
    .social-icon-link:hover {
      color: #10B981;
    }
    ```
    If it already exists, leave it. If missing or incomplete, add/update it.

    ACCENT CONSISTENCY AUDIT (POLSH-03):
    Do a quick scan for emerald usage across components to ensure at least 3 distinct element types use the emerald accent visually:
    1. Hero photo circular outline (`outline: 2px solid #10B981`) — should already exist in HeroSection
    2. Social icon links hover state (`color: #10B981`) — in HeroSection and ContactSection
    3. Card hover glow (`box-shadow: 0 0 12px rgba(16,185,129,0.35)`) — now in all card types

    If any of these are missing, add them. Do NOT add emerald as a text color for body paragraphs or label text — WCAG AA contrast failure risk (3.8:1 ratio vs required 4.5:1 for normal text). Emerald is for large elements and decorative accents only.

    Run the POLSH-03 smoke test to confirm emerald appears on 3+ element types.
  </action>
  <verify>
    <automated>npx playwright test tests/phase5.spec.ts --grep "POLSH-02|POLSH-03" --project=chromium 2>&1 | tail -15</automated>
  </verify>
  <done>ProjectsSection project cards have translateY(-4px) + glow + transition. ContactSection social icon links turn emerald on hover. Emerald accent visible on 3+ distinct element types. POLSH-02 and POLSH-03 smoke tests pass GREEN.</done>
</task>

</tasks>

<verification>
npx playwright test tests/phase5.spec.ts --grep "POLSH-02|POLSH-03" --project=chromium
Expected: Both POLSH-02 and POLSH-03 pass GREEN.
</verification>

<success_criteria>
All four card types (skill cards, project cards, experience entries, social icon links) have lift+glow hover states using translateY(-4px) and box-shadow: 0 0 12px rgba(16,185,129,0.35) with 150-200ms transition. Emerald #10B981 appears on 3+ distinct element types. POLSH-02 and POLSH-03 smoke tests GREEN.
</success_criteria>

<output>
After completion, create .planning/phases/05-seo-polish-launch/05-04-SUMMARY.md
</output>
