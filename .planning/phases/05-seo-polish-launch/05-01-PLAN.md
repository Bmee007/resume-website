---
phase: 05-seo-polish-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase5.spec.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05
  - SEO-06
  - POLSH-01
  - POLSH-02
  - POLSH-03
  - POLSH-04
  - POLSH-05

must_haves:
  truths:
    - "All SEO and Polish requirements have corresponding automated smoke tests"
    - "Tests fail (RED) before implementation — confirming they test real behavior"
    - "SEO-04 and POLSH-04 are documented as manual-only with no false automated tests"
  artifacts:
    - path: "tests/phase5.spec.ts"
      provides: "9 smoke tests covering SEO-01/02/03/05/06 and POLSH-01/02/03/05"
      min_lines: 80
  key_links:
    - from: "tests/phase5.spec.ts"
      to: "npx playwright test tests/phase5.spec.ts --project=chromium"
      via: "Playwright test runner"
      pattern: "test.*SEO-0[12356]|POLSH-0[1235]"
---

<objective>
Create Wave 0 Playwright smoke tests for all Phase 5 requirements, establishing RED state before any implementation begins.

Purpose: TDD guard — tests define the observable truth for each requirement. Subsequent Wave 1 plans implement against these tests.
Output: tests/phase5.spec.ts with 9 RED smoke tests.
</objective>

<execution_context>
@/Users/mylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-seo-polish-launch/05-CONTEXT.md
@.planning/phases/05-seo-polish-launch/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Phase 5 smoke tests (RED state)</name>
  <files>tests/phase5.spec.ts</files>
  <behavior>
    - SEO-01: page title contains "ERP" AND "AI"; meta[name=description] exists and is non-empty
    - SEO-02: page head contains script[type="application/ld+json"] whose text includes "@type":"Person" and sameAs with linkedin.com and github.com
    - SEO-03: meta[property="og:image"], meta[property="og:title"], meta[property="og:description"] all present in head
    - SEO-05: every img element has a non-empty alt attribute (query all imgs, assert alt length > 0 for each)
    - SEO-06: GET /sitemap-index.xml returns HTTP 200; GET /robots.txt returns 200 and body includes "Sitemap:"
    - POLSH-01: at least one .section-animate element exists; after scrolling into view it gains class "is-visible"; element has opacity > 0 after class applied
    - POLSH-02: .skill-card has CSS transition property containing "transform"; hovering .skill-card adds translateY transform; .project-card and .entry-card similarly respond to hover
    - POLSH-03: emerald color #10B981 or rgb(16, 185, 129) appears in computed styles on at least 3 distinct element types (e.g., photo outline, social icon hover, section heading accent)
    - POLSH-05: navigating to /nonexistent-path returns a page that contains Nav (selector .desktop-nav or nav[aria-label]) and a link to "/" (home CTA)
    - SEO-04 and POLSH-04: add comments marking these as manual-only — NO automated test created for them
  </behavior>
  <action>
    Create tests/phase5.spec.ts. Use Playwright test structure matching prior phase test files in the tests/ directory. Each test is tagged with its requirement ID in the test name (e.g., "SEO-01: page title and meta description").

    Import pattern: Use the same import style as existing phase spec files (check tests/ for reference — likely `import { test, expect } from '@playwright/test'`).

    Base URL: Use `http://localhost:4321` (Astro dev server default). Tests run against `npm run dev`.

    POLSH-01 scroll test: Use page.locator('.section-animate').first(), then page.evaluate(() => window.scrollBy(0, 500)) to scroll, then wait for the is-visible class with waitForFunction or locator assertion.

    POLSH-02 hover test: Use page.hover('.skill-card') then page.evaluate to check computed transform style. Check transition string includes "transform" via getComputedStyle.

    POLSH-03 accent test: Check at least 3 of: hero-photo outline color, social-icon-link:hover color, any element with color #10B981 in computed style. Use page.evaluate to query computed styles.

    POLSH-05 404 test: Navigate to /this-page-does-not-exist and assert page contains a nav element and an anchor pointing to "/".

    SEO-06: Use page.request.get('/sitemap-index.xml') and page.request.get('/robots.txt') with status() assertions.

    After writing: run `npx playwright test tests/phase5.spec.ts --project=chromium` and confirm tests FAIL (RED state). If any tests pass before implementation, tighten the assertion.
  </action>
  <verify>
    <automated>npx playwright test tests/phase5.spec.ts --project=chromium 2>&1 | tail -20</automated>
  </verify>
  <done>tests/phase5.spec.ts exists with 9 describe/test blocks. Running the suite shows 9 failed tests (RED). No tests pass before implementation. SEO-04 and POLSH-04 are noted as manual-only in comments.</done>
</task>

</tasks>

<verification>
Run: npx playwright test tests/phase5.spec.ts --project=chromium
Expected: all 9 tests FAIL (RED state confirms tests are checking real, not-yet-implemented behavior)
</verification>

<success_criteria>
tests/phase5.spec.ts exists with 9 RED smoke tests covering SEO-01/02/03/05/06 and POLSH-01/02/03/05. No false passes.
</success_criteria>

<output>
After completion, create .planning/phases/05-seo-polish-launch/05-01-SUMMARY.md
</output>
