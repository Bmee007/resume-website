---
phase: 05-seo-polish-launch
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/og-image.png
autonomous: false
gap_closure: true
requirements: [SEO-03, SEO-05]

must_haves:
  truths:
    - "GET /og-image.png returns HTTP 200 from the dev server"
    - "OG card previews on LinkedIn/Twitter render a branded image (not a broken image icon)"
    - "SEO-05 smoke test passes green with zero failures"
  artifacts:
    - path: "public/og-image.png"
      provides: "1200x630 branded OG image asset"
      contains: "PNG file, minimum 1200x630px"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "public/og-image.png"
      via: "og:image meta tag defaulting to /og-image.png"
      pattern: "ogImage.*og-image\\.png"
---

<objective>
Close the single remaining automated gap in Phase 5: the missing `public/og-image.png` asset.

Purpose: The BaseLayout already wires og:image to /og-image.png and the SEO-05 smoke test asserts HTTP 200 for that path. Without the file, every LinkedIn/Twitter share of the site renders a broken image card and the automated test fails. This plan provides the exact spec for the image and verifies the gap is closed after the human creates it.

Output: `public/og-image.png` — a 1200x630 branded PNG with dark navy background, Borina's name in white, and her title in emerald. SEO-05 smoke test passes green.
</objective>

<execution_context>
@/Users/mylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-polish-launch/05-05-SUMMARY.md

<interfaces>
<!-- Key wiring already in place — executor context only, no code changes needed -->

From src/layouts/BaseLayout.astro (line 19):
  ogImage prop defaults to "/og-image.png"
  ogImageAbsolute = new URL(ogImage, siteBase).href  → absolute URL for og:image meta tag

From tests/phase5.spec.ts (lines 98-100):
  const ogImageResponse = await page.request.get("/og-image.png");
  expect(ogImageResponse.status()).toBe(200);
  // Test runs against dev server (npx astro dev) — file must exist in public/
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Create public/og-image.png (1200x630 branded PNG)</name>
  <files>public/og-image.png</files>
  <action>
    Create a 1200x630 PNG using Figma, Canva (canva.com/create/social-media), or any image editor.
    Claude cannot generate binary image files — this is a required human action.

    Spec:
    - Canvas: 1200 x 630 pixels
    - Background: #0F172A (dark navy — matches site background)
    - Primary text: "Borina Keo" — #FFFFFF white, ~72px, weight 600, upper area with ~80px padding
    - Secondary text: "ERP & WMS AI Integration Leader" — #10B981 emerald, ~36px, weight 400, ~16px below name
    - Optional: thin emerald (#10B981) vertical stripe on the left edge (8-12px wide) for brand accent
    - No photo needed — text-only card reads better at LinkedIn thumbnail size

    Steps:
    1. Open Figma, Canva, or any image editor
    2. Create 1200 x 630 px canvas
    3. Fill background with #0F172A
    4. Add "Borina Keo" in white (~72px, weight 600)
    5. Add "ERP & WMS AI Integration Leader" in #10B981 emerald (~36px) below the name
    6. Export as PNG
    7. Save to: /Users/mylinh/projects/resume-website/public/og-image.png
  </action>
  <verify>ls -lh /Users/mylinh/projects/resume-website/public/og-image.png</verify>
  <done>public/og-image.png exists as a valid PNG file at least 5KB in size.</done>
  <resume-signal>Type "done" when public/og-image.png has been placed, or describe any issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify SEO-05 smoke test passes with the placed asset</name>
  <files>public/og-image.png</files>
  <action>
    Confirm the asset exists at the correct path, then run the SEO-05 smoke test against the dev server to verify the HTTP 200 assertion passes. The dev server serves public/ at the root, so public/og-image.png is accessible at /og-image.png.

    Steps:
    1. Verify file exists and is non-empty: `ls -lh public/og-image.png` (expect >5KB for a 1200x630 PNG)
    2. Start the dev server if not already running: `npx astro dev --port 4321`
    3. Run targeted SEO-05 smoke test:
       `npx playwright test tests/phase5.spec.ts --grep "SEO-05" --project=desktop`
    4. Run full Phase 5 suite to confirm no regressions:
       `npx playwright test tests/phase5.spec.ts --project=desktop`

    Do NOT modify any source files. This task is verification only.

    Note: Use --project=desktop (not --project=chromium — that project name does not exist in playwright.config.ts).
  </action>
  <verify>
    <automated>npx playwright test tests/phase5.spec.ts --grep "SEO-05" --project=desktop</automated>
  </verify>
  <done>SEO-05 test passes green (HTTP 200 for /og-image.png). Full Phase 5 suite shows all 9 tests passing — SEO-05 flips from fail to pass with no other regressions.</done>
</task>

</tasks>

<verification>
After Task 2 completes:
- `npx playwright test tests/phase5.spec.ts --project=desktop` — all 9 tests green
- `ls -lh public/og-image.png` — file exists, size > 5KB
- `file public/og-image.png` — confirms PNG format
</verification>

<success_criteria>
1. public/og-image.png exists as a valid PNG, 1200x630px, with dark navy background and branded text
2. SEO-05 smoke test passes: GET /og-image.png returns HTTP 200 from the dev server
3. Full Phase 5 Playwright suite passes (all 9 automated tests green)
4. OG card preview on LinkedIn/Twitter will render correctly once deployed (requires public deployment — manual verification)
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-polish-launch/05-06-SUMMARY.md`
</output>
