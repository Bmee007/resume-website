---
// AboutSection.astro — Bento grid layout
const stats = [
  { value: 15, suffix: "+", label: "Years in Enterprise AI & Ops" },
  { value: 3,  suffix: "+", label: "Industries: Supply Chain, Healthcare, Retail" },
  { value: 5,  suffix: "+", label: "Autonomous Agent Systems Deployed" },
];
---

<section id="about" class="about-root">

  <span class="section-num" aria-hidden="true">01</span>

  <div class="about-container">

    <!-- Section header -->
    <div class="about-header" data-reveal-heading>
      <span class="eyebrow-accent">About</span>
      <h2 class="display-serif about-heading">
        AI Systems<br/>Engineer &amp;<br/>Agent Builder
      </h2>
    </div>

    <!-- Bento grid -->
    <div class="bento-grid">

      <!-- Bio card — spans 2 cols on large screens -->
      <div class="bento-card bento-bio" data-reveal>
        <p class="bio-lead">
          I engineer AI systems that operate at enterprise scale — building autonomous
          agents with custom harnesses, skills, and workflows, grounded in 15+ years of
          supply chain, healthcare, and retail domain expertise.
        </p>
        <p class="bio-body">
          My foundation is data science and business operations: I've led ERP, WMS, and TMS
          transformations across supply chain, healthcare distribution, and retail
          networks — understanding the messy, real-world data that AI models need to
          actually work. That operational depth is what separates systems that demo
          well from ones that survive production.
        </p>
        <p class="bio-body">
          Now I focus on the frontier: designing agent architectures with harnesses,
          skills, and multi-step workflows; researching LLM memory systems (episodic,
          semantic, and working memory); and investigating agent security — prompt
          injection, trust boundaries, and authorization patterns for autonomous systems.
          The goal is AI that is not just capable, but reliable and safe.
        </p>
        <div class="bio-badge">
          <span class="badge-dot" aria-hidden="true"></span>
          Currently: AI &amp; Agent Systems Lead · Kohl and Frisch
        </div>
      </div>

      <!-- Stat cards -->
      {stats.map((stat) => (
        <div class="bento-card bento-stat" data-reveal>
          <span
            class="stat-num"
            data-count-to={stat.value}
            data-count-suffix={stat.suffix}
          >0{stat.suffix}</span>
          <span class="stat-label">{stat.label}</span>
        </div>
      ))}

      <!-- Visual accent card -->
      <div class="bento-card bento-visual" data-reveal aria-hidden="true">
        <div class="bento-bar"></div>
        <div class="bento-tags">
          <span class="bento-tag bento-tag--amber">Agents</span>
          <span class="bento-tag bento-tag--amber">AI</span>
          <span class="bento-tag">Supply Chain</span>
          <span class="bento-tag bento-tag--amber">Security</span>
          <span class="bento-tag">ERP · WMS · TMS</span>
          <span class="bento-tag">Data Science</span>
          <span class="bento-tag">Python</span>
        </div>
      </div>

    </div>

  </div>
</section>

<style>
  .about-root {
    position: relative;
    padding: clamp(6rem, 10vw, 9rem) clamp(1.5rem, 5vw, 4rem);
    overflow: hidden;
  }

  .about-container {
    max-width: 80rem;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  /* ── Header ───────────────────────────────────────────────── */
  .about-header {
    margin-bottom: clamp(3rem, 5vw, 5rem);
  }

  .about-heading {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    margin: 0;
  }

  /* ── Bento grid ───────────────────────────────────────────── */
  .bento-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
  }

  @media (min-width: 640px) {
    .bento-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .bento-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  /* ── Cards ────────────────────────────────────────────────── */
  .bento-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 1rem;
    padding: 2rem;
    transition:
      border-color 0.3s var(--ease-expo-out),
      transform    0.3s var(--ease-expo-out),
      box-shadow   0.3s var(--ease-expo-out);
  }

  .bento-card:hover {
    border-color: var(--border-strong);
    transform: translateY(-3px);
    box-shadow: 0 12px 40px rgba(0,0,0,0.08);
  }

  /* Bio: spans 2 cols on desktop */
  .bento-bio {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  @media (min-width: 1024px) {
    .bento-bio {
      grid-column: 1 / 3;
      grid-row: 1 / 3;
    }
  }

  /* Stats */
  .bento-stat {
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    gap: 0.375rem;
    min-height: 140px;
  }

  /* Visual card: bottom-right on desktop */
  .bento-visual {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    justify-content: space-between;
    min-height: 140px;
  }

  @media (min-width: 1024px) {
    .bento-visual {
      grid-column: 4;
      grid-row: 2;
    }
  }

  /* ── Bio text ─────────────────────────────────────────────── */
  .bio-lead {
    font-size: 1.0625rem;
    color: var(--foreground);
    line-height: 1.75;
    margin: 0;
    font-weight: 400;
  }

  .bio-body {
    font-size: 0.9375rem;
    color: var(--fg-muted);
    line-height: 1.8;
    margin: 0;
  }

  .bio-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.8125rem;
    color: var(--fg-muted);
    margin-top: 0.25rem;
  }

  .badge-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--amber);
    flex-shrink: 0;
    animation: badge-pulse 2.5s ease-in-out infinite;
  }

  @keyframes badge-pulse {
    0%, 100% { opacity: 1; box-shadow: 0 0 0 0 var(--amber-dim); }
    50%       { opacity: 0.7; box-shadow: 0 0 0 4px transparent; }
  }

  /* ── Stat ─────────────────────────────────────────────────── */
  .stat-num {
    font-family: "DM Serif Display", Georgia, serif;
    font-style: italic;
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    color: var(--amber);
    line-height: 1;
    letter-spacing: -0.02em;
    display: block;
  }

  .stat-label {
    font-size: 0.8125rem;
    color: var(--fg-muted);
    line-height: 1.4;
    font-weight: 400;
  }

  /* ── Visual card internals ────────────────────────────────── */
  .bento-bar {
    height: 2px;
    background: linear-gradient(
      to right,
      var(--amber),
      var(--amber-glow),
      transparent
    );
    border-radius: 2px;
  }

  .bento-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .bento-tag {
    font-size: 0.6875rem;
    font-weight: 600;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    color: var(--fg-subtle);
    border: 1px solid var(--border);
    border-radius: 0.25rem;
    padding: 0.2rem 0.5rem;
  }

  .bento-tag--amber {
    color: var(--amber);
    border-color: color-mix(in oklch, var(--amber) 30%, transparent);
  }

  /* ── Reveal animation (GSAP uses data-reveal; CSS fallback) ─ */
  [data-reveal] {
    opacity: 0;
    transform: translateY(32px);
    transition:
      opacity   0.7s var(--ease-expo-out),
      transform 0.7s var(--ease-expo-out);
  }

  [data-reveal].is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  [data-reveal-heading] {
    opacity: 0;
    transform: translateX(-24px);
    transition:
      opacity   0.8s var(--ease-expo-out),
      transform 0.8s var(--ease-expo-out);
  }

  [data-reveal-heading].is-visible {
    opacity: 1;
    transform: translateX(0);
  }

  @media (prefers-reduced-motion: reduce) {
    [data-reveal],
    [data-reveal-heading] {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }
</style>
